describe('authentication', () => {
	describe('sign-up', () => {
		it('should create an account if the detail does not exist within the databsae', () => {

		})
		it('should not enable creation of a user with no password', () => {

		})
		it('should not enable creation if the username is in use', () => {

		})
	})
	describe('login', () => {
		it('should login with a normal username and password (default admin as described in the document', () => {

		})
		it('should not enable login with a google account username', () => {

		})
		it('should not save the password to the database', () => {

		})
	})
})
