#!/usr/bin/env bash

echo "installing postgres"
sudo apt install postgresql -y

echo "edit either '/etc/postgresql/$\VER/main/pg_hba.conf' (deb) or '/var/lib/pgsql/$\VER/data/pg_hba.conf' (rhel)"
echo "change:"
echo "local   all             all                                      peer"
echo "to"
echo "local   all             all                                      md5"

# createuser -EPd fyp
login to db as postgres and do:
CREATE DATABASE fyp;
ALTER USER fyp PASSWORD 'fyp';